final aggregation over ()
    local exchange (GATHER, SINGLE, [])
        remote exchange (GATHER, SINGLE, [])
            partial aggregation over ()
                filter ((("shipdate" >= DATE '1994-01-01') AND ("shipdate" < DATE '1995-01-01')) AND (("discount" BETWEEN 5E-2 AND 7E-2) AND ("quantity" < 2.4E1)))
                    scan lineitem
