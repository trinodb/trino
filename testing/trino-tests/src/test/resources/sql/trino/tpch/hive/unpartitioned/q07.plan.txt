remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            final aggregation over (expr_gid, name_21, name_27)
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [expr_gid, name_21, name_27])
                        partial aggregation over (expr_gid, name_21, name_27)
                            join (INNER, PARTITIONED):
                                remote exchange (REPARTITION, HASH, [orderkey])
                                    join (INNER, REPLICATED):
                                        dynamic filter ([Reference[type=bigint, name=orderkey], Reference[type=bigint, name=suppkey_0]])
                                            scan lineitem
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPLICATE, BROADCAST, [])
                                                join (INNER, REPLICATED):
                                                    dynamic filter (await [Reference[type=bigint, name=nationkey]])
                                                        scan supplier
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            scan nation
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [orderkey_5])
                                        join (INNER, PARTITIONED):
                                            remote exchange (REPARTITION, HASH, [custkey])
                                                dynamic filter ([Reference[type=bigint, name=custkey]])
                                                    scan orders
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPARTITION, HASH, [custkey_10])
                                                    join (INNER, REPLICATED):
                                                        dynamic filter (await [Reference[type=bigint, name=nationkey_13]])
                                                            scan customer
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPLICATE, BROADCAST, [])
                                                                scan nation
