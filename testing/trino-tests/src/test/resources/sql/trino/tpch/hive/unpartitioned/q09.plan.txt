remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            final aggregation over (expr$gid, name_23)
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [expr$gid, name_23])
                        partial aggregation over (expr$gid, name_23)
                            join (INNER, REPLICATED):
                                join (INNER, PARTITIONED):
                                    remote exchange (REPARTITION, HASH, [orderkey])
                                        join (INNER, PARTITIONED):
                                            join (INNER, PARTITIONED):
                                                remote exchange (REPARTITION, HASH, [suppkey_6])
                                                    join (INNER, PARTITIONED):
                                                        remote exchange (REPARTITION, HASH, [partkey_5])
                                                            dynamic filter ([SymbolReference[type=bigint, name=orderkey], SymbolReference[type=bigint, name=partkey_5], SymbolReference[type=bigint, name=partkey_5], SymbolReference[type=bigint, name=suppkey_6], SymbolReference[type=bigint, name=suppkey_6]])
                                                                scan lineitem
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPARTITION, HASH, [partkey])
                                                                dynamic filter ([SymbolReference[type=bigint, name=partkey]])
                                                                    scan part
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [suppkey])
                                                        dynamic filter ([SymbolReference[type=bigint, name=suppkey]], await [SymbolReference[type=bigint, name=nationkey]])
                                                            scan supplier
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPARTITION, HASH, [suppkey_12])
                                                    scan partsupp
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [orderkey_17])
                                            scan orders
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPLICATE, BROADCAST, [])
                                        scan nation
