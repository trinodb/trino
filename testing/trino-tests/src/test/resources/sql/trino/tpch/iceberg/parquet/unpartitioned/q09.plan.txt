remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            final aggregation over (expr_gid, name_19)
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [expr_gid, name_19])
                        partial aggregation over (expr_gid, name_19)
                            join (INNER, REPLICATED):
                                join (INNER, PARTITIONED):
                                    remote exchange (REPARTITION, HASH, [orderkey])
                                        join (INNER, PARTITIONED):
                                            join (INNER, PARTITIONED):
                                                remote exchange (REPARTITION, HASH, [suppkey_5])
                                                    join (INNER, PARTITIONED):
                                                        remote exchange (REPARTITION, HASH, [partkey_4])
                                                            dynamic filter ([Reference[type=bigint, name=orderkey], Reference[type=bigint, name=partkey_4], Reference[type=bigint, name=partkey_4], Reference[type=bigint, name=suppkey_5], Reference[type=bigint, name=suppkey_5]])
                                                                scan lineitem
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPARTITION, HASH, [partkey])
                                                                dynamic filter ([Reference[type=bigint, name=partkey]])
                                                                    scan part
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [suppkey])
                                                        dynamic filter ([Reference[type=bigint, name=suppkey]], await [Reference[type=bigint, name=nationkey]])
                                                            scan supplier
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPARTITION, HASH, [suppkey_10])
                                                    scan partsupp
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [orderkey_14])
                                            scan orders
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPLICATE, BROADCAST, [])
                                        scan nation
