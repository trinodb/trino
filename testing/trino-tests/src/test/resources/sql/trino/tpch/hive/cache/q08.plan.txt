remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            final aggregation over (expr$gid)
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [expr$gid])
                        partial aggregation over (expr$gid)
                            join (INNER, PARTITIONED):
                                remote exchange (REPARTITION, HASH, [suppkey_6])
                                    join (INNER, PARTITIONED):
                                        remote exchange (REPARTITION, HASH, [custkey])
                                            join (INNER, PARTITIONED):
                                                remote exchange (REPARTITION, HASH, [orderkey_11])
                                                    alternatives
                                                        dynamic filter ([SymbolReference[type=bigint, name=custkey], SymbolReference[type=bigint, name=orderkey_11]])
                                                            scan orders
                                                        cache data
                                                            dynamic filter ([SymbolReference[type=bigint, name=custkey], SymbolReference[type=bigint, name=orderkey_11]])
                                                                scan orders
                                                        load from cache
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [orderkey])
                                                        join (INNER, REPLICATED):
                                                            alternatives
                                                                dynamic filter ([SymbolReference[type=bigint, name=partkey_5], SymbolReference[type=bigint, name=suppkey_6]])
                                                                    scan lineitem
                                                                cache data
                                                                    dynamic filter ([SymbolReference[type=bigint, name=partkey_5], SymbolReference[type=bigint, name=suppkey_6]])
                                                                        scan lineitem
                                                                load from cache
                                                            local exchange (GATHER, SINGLE, [])
                                                                remote exchange (REPLICATE, BROADCAST, [])
                                                                    alternatives
                                                                        scan part
                                                                        cache data
                                                                            scan part
                                                                        load from cache
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPARTITION, HASH, [custkey_16])
                                                join (INNER, REPLICATED):
                                                    alternatives
                                                        dynamic filter (await [SymbolReference[type=bigint, name=nationkey_19]])
                                                            scan customer
                                                        cache data
                                                            dynamic filter (await [SymbolReference[type=bigint, name=nationkey_19]])
                                                                scan customer
                                                        load from cache
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            join (INNER, REPLICATED):
                                                                alternatives
                                                                    dynamic filter (await [SymbolReference[type=bigint, name=regionkey]])
                                                                        scan nation
                                                                    cache data
                                                                        dynamic filter (await [SymbolReference[type=bigint, name=regionkey]])
                                                                            scan nation
                                                                    load from cache
                                                                local exchange (GATHER, SINGLE, [])
                                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                                        alternatives
                                                                            scan region
                                                                            cache data
                                                                                scan region
                                                                            load from cache
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [suppkey])
                                        join (INNER, REPLICATED):
                                            alternatives
                                                dynamic filter (await [SymbolReference[type=bigint, name=nationkey]])
                                                    scan supplier
                                                cache data
                                                    dynamic filter (await [SymbolReference[type=bigint, name=nationkey]])
                                                        scan supplier
                                                load from cache
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    alternatives
                                                        scan nation
                                                        cache data
                                                            scan nation
                                                        load from cache
