local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                final aggregation over (name, custkey_0, orderkey_6, orderdate, totalprice)
                    local exchange (GATHER, SINGLE, [])
                        join (INNER, PARTITIONED):
                            remote exchange (REPARTITION, HASH, [orderkey_6])
                                partial aggregation over (orderkey_6)
                                    dynamic filter (orderkey_6::EQUAL)
                                        scan lineitem
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [orderkey])
                                    join (INNER, PARTITIONED):
                                        remote exchange (REPARTITION, HASH, [custkey_0])
                                            join (INNER, PARTITIONED):
                                                remote exchange (REPARTITION, HASH, [orderkey])
                                                    dynamic filter (custkey_0::EQUAL, orderkey::EQUAL)
                                                        scan orders
                                                final aggregation over (orderkey_12)
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPARTITION, HASH, [orderkey_12])
                                                            partial aggregation over (orderkey_12)
                                                                scan lineitem
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPARTITION, HASH, [custkey])
                                                scan customer
