remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            join (INNER, PARTITIONED):
                final aggregation over (suppkey_7)
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPARTITION, HASH, [suppkey_7])
                            partial aggregation over (suppkey_7)
                                cross join (can skip output duplicates):
                                    join (RIGHT, PARTITIONED, can skip output duplicates):
                                        final aggregation over (partkey_24, suppkey_25)
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPARTITION, HASH, [partkey_24, suppkey_25])
                                                    partial aggregation over (partkey_24, suppkey_25)
                                                        dynamic filter (suppkey_25::EQUAL, partkey_24::EQUAL, suppkey_25::EQUAL)
                                                            scan lineitem
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPARTITION, HASH, [partkey, suppkey_7])
                                                join (INNER, PARTITIONED, can skip output duplicates):
                                                    remote exchange (REPARTITION, HASH, [partkey])
                                                        dynamic filter (suppkey_7::EQUAL, partkey::EQUAL)
                                                            scan partsupp
                                                    final aggregation over (partkey_15)
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPARTITION, HASH, [partkey_15])
                                                                partial aggregation over (partkey_15)
                                                                    scan part
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            single aggregation over ()
                                                values (1 rows)
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [suppkey])
                        join (INNER, REPLICATED):
                            dynamic filter (nationkey::EQUAL)
                                scan supplier
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    scan nation
