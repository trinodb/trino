remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            join (INNER, PARTITIONED):
                final aggregation over (suppkey_8)
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPARTITION, HASH, [suppkey_8])
                            partial aggregation over (suppkey_8)
                                cross join (can skip output duplicates):
                                    join (RIGHT, PARTITIONED, can skip output duplicates):
                                        final aggregation over (partkey_27, suppkey_28)
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPARTITION, HASH, [partkey_27, suppkey_28])
                                                    partial aggregation over (partkey_27, suppkey_28)
                                                        dynamic filter (partkey_27::EQUAL, suppkey_28::EQUAL, suppkey_28::EQUAL)
                                                            scan lineitem
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPARTITION, HASH, [partkey, suppkey_8])
                                                join (INNER, PARTITIONED, can skip output duplicates):
                                                    remote exchange (REPARTITION, HASH, [partkey])
                                                        dynamic filter (partkey::EQUAL, suppkey_8::EQUAL)
                                                            scan partsupp
                                                    final aggregation over (partkey_17)
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPARTITION, HASH, [partkey_17])
                                                                partial aggregation over (partkey_17)
                                                                    scan part
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            single aggregation over ()
                                                values (1 rows)
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [suppkey])
                        join (INNER, REPLICATED):
                            dynamic filter (nationkey::EQUAL)
                                scan supplier
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    scan nation
