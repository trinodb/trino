local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                final aggregation over (cc_name, gid, sm_type)
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPARTITION, HASH, [cc_name, gid, sm_type])
                            intermediate aggregation over (cc_name, gid, sm_type)
                                join (INNER, REPLICATED):
                                    join (INNER, REPLICATED):
                                        join (INNER, REPLICATED):
                                            partial aggregation over (cs_call_center_sk, cs_ship_mode_sk, cs_warehouse_sk)
                                                join (INNER, REPLICATED):
                                                    alternatives
                                                        dynamic filter (await [cs_call_center_sk::bigint, cs_ship_date_sk::bigint, cs_ship_mode_sk::bigint, cs_warehouse_sk::bigint])
                                                            scan catalog_sales
                                                        cache data
                                                            dynamic filter (await [cs_call_center_sk::bigint, cs_ship_date_sk::bigint, cs_ship_mode_sk::bigint, cs_warehouse_sk::bigint])
                                                                scan catalog_sales
                                                        load from cache
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            alternatives
                                                                scan date_dim
                                                                cache data
                                                                    scan date_dim
                                                                load from cache
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    alternatives
                                                        scan ship_mode
                                                        cache data
                                                            scan ship_mode
                                                        load from cache
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPLICATE, BROADCAST, [])
                                                alternatives
                                                    scan call_center
                                                    cache data
                                                        scan call_center
                                                    load from cache
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            alternatives
                                                scan warehouse
                                                cache data
                                                    scan warehouse
                                                load from cache
