final aggregation over ()
    local exchange (GATHER, SINGLE, [])
        remote exchange (GATHER, SINGLE, [])
            partial aggregation over ()
                join (INNER, PARTITIONED):
                    remote exchange (REPARTITION, HASH, [cs_bill_customer_sk])
                        join (INNER, PARTITIONED):
                            remote exchange (REPARTITION, HASH, [cs_item_sk])
                                join (INNER, REPLICATED):
                                    dynamic filter (unestimatable [cs_bill_customer_sk::bigint, cs_item_sk::bigint], await [cs_sold_date_sk::bigint])
                                        scan catalog_sales
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            scan date_dim
                            final aggregation over (ss_item_sk)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [ss_item_sk])
                                        partial aggregation over (ss_item_sk)
                                            final aggregation over (d_date_6, gid, ss_item_sk)
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [d_date_6, gid, ss_item_sk])
                                                        partial aggregation over (d_date_6, gid, ss_item_sk)
                                                            join (INNER, REPLICATED):
                                                                join (INNER, REPLICATED):
                                                                    dynamic filter ([ss_item_sk::bigint], await [ss_sold_date_sk::bigint])
                                                                        scan store_sales
                                                                    local exchange (GATHER, SINGLE, [])
                                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                                            scan date_dim
                                                                local exchange (GATHER, SINGLE, [])
                                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                                        scan item
                    single aggregation over (ss_customer_sk_44)
                        cross join (can skip output duplicates):
                            final aggregation over (ss_customer_sk_44)
                                local exchange (GATHER, SINGLE, [])
                                    partial aggregation over (ss_customer_sk_44)
                                        join (INNER, PARTITIONED):
                                            remote exchange (REPARTITION, HASH, [ss_customer_sk_44])
                                                dynamic filter ([ss_customer_sk_44::bigint])
                                                    scan store_sales
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPARTITION, HASH, [c_customer_sk])
                                                    scan customer
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    final aggregation over ()
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (GATHER, SINGLE, [])
                                                partial aggregation over ()
                                                    final aggregation over (ss_customer_sk_72)
                                                        local exchange (GATHER, SINGLE, [])
                                                            partial aggregation over (ss_customer_sk_72)
                                                                join (INNER, REPLICATED):
                                                                    join (INNER, PARTITIONED):
                                                                        remote exchange (REPARTITION, HASH, [ss_customer_sk_72])
                                                                            dynamic filter ([ss_customer_sk_72::bigint], await [ss_sold_date_sk_69::bigint])
                                                                                scan store_sales
                                                                        local exchange (GATHER, SINGLE, [])
                                                                            remote exchange (REPARTITION, HASH, [c_customer_sk_94])
                                                                                scan customer
                                                                    local exchange (GATHER, SINGLE, [])
                                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                                            scan date_dim
            partial aggregation over ()
                join (INNER, PARTITIONED):
                    remote exchange (REPARTITION, HASH, [ws_bill_customer_sk])
                        join (INNER, PARTITIONED):
                            remote exchange (REPARTITION, HASH, [ws_item_sk])
                                join (INNER, REPLICATED):
                                    dynamic filter (unestimatable [ws_bill_customer_sk::bigint, ws_item_sk::bigint], await [ws_sold_date_sk::bigint])
                                        scan web_sales
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            scan date_dim
                            final aggregation over (ss_item_sk_185)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [ss_item_sk_185])
                                        partial aggregation over (ss_item_sk_185)
                                            final aggregation over (d_date_210, gid_263, ss_item_sk_185)
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [d_date_210, gid_263, ss_item_sk_185])
                                                        partial aggregation over (d_date_210, gid_263, ss_item_sk_185)
                                                            join (INNER, REPLICATED):
                                                                join (INNER, REPLICATED):
                                                                    dynamic filter ([ss_item_sk_185::bigint], await [ss_sold_date_sk_183::bigint])
                                                                        scan store_sales
                                                                    local exchange (GATHER, SINGLE, [])
                                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                                            scan date_dim
                                                                local exchange (GATHER, SINGLE, [])
                                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                                        scan item
                    single aggregation over (ss_customer_sk_272)
                        cross join (can skip output duplicates):
                            final aggregation over (ss_customer_sk_272)
                                local exchange (GATHER, SINGLE, [])
                                    partial aggregation over (ss_customer_sk_272)
                                        join (INNER, PARTITIONED):
                                            remote exchange (REPARTITION, HASH, [ss_customer_sk_272])
                                                dynamic filter ([ss_customer_sk_272::bigint])
                                                    scan store_sales
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPARTITION, HASH, [c_customer_sk_294])
                                                    scan customer
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    final aggregation over ()
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (GATHER, SINGLE, [])
                                                partial aggregation over ()
                                                    final aggregation over (ss_customer_sk_319)
                                                        local exchange (GATHER, SINGLE, [])
                                                            partial aggregation over (ss_customer_sk_319)
                                                                join (INNER, REPLICATED):
                                                                    join (INNER, PARTITIONED):
                                                                        remote exchange (REPARTITION, HASH, [ss_customer_sk_319])
                                                                            dynamic filter ([ss_customer_sk_319::bigint], await [ss_sold_date_sk_316::bigint])
                                                                                scan store_sales
                                                                        local exchange (GATHER, SINGLE, [])
                                                                            remote exchange (REPARTITION, HASH, [c_customer_sk_341])
                                                                                scan customer
                                                                    local exchange (GATHER, SINGLE, [])
                                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                                            scan date_dim
