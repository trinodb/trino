remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            join (INNER, PARTITIONED):
                join (INNER, PARTITIONED):
                    single aggregation over (d_week_seq)
                        final aggregation over (d_day_name, d_week_seq)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [d_week_seq])
                                    intermediate aggregation over (d_day_name, d_week_seq)
                                        join (INNER, REPLICATED):
                                            local exchange (REPARTITION, ROUND_ROBIN, [])
                                                partial aggregation over (ws_sold_date_sk)
                                                    dynamic filter (await [Reference[type=bigint, name=ws_sold_date_sk]])
                                                        scan web_sales
                                                partial aggregation over (cs_sold_date_sk)
                                                    dynamic filter (await [Reference[type=bigint, name=cs_sold_date_sk]])
                                                        scan catalog_sales
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    dynamic filter ([Reference[type=integer, name=d_week_seq]], await [Reference[type=integer, name=d_week_seq]])
                                                        scan date_dim
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPARTITION, HASH, [d_week_seq_22])
                            dynamic filter ([Reference[type=integer, name=d_week_seq_22]])
                                scan date_dim
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [operator_subtract])
                        join (INNER, PARTITIONED):
                            single aggregation over (d_week_seq_129)
                                final aggregation over (d_day_name_139, d_week_seq_129)
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [d_week_seq_129])
                                            intermediate aggregation over (d_day_name_139, d_week_seq_129)
                                                join (INNER, REPLICATED):
                                                    local exchange (REPARTITION, ROUND_ROBIN, [])
                                                        partial aggregation over (ws_sold_date_sk_51)
                                                            dynamic filter (await [Reference[type=bigint, name=ws_sold_date_sk_51]])
                                                                scan web_sales
                                                        partial aggregation over (cs_sold_date_sk_88)
                                                            dynamic filter (await [Reference[type=bigint, name=cs_sold_date_sk_88]])
                                                                scan catalog_sales
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            dynamic filter (await [Reference[type=integer, name=d_week_seq_129]])
                                                                scan date_dim
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [d_week_seq_174])
                                    scan date_dim
