local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                join (LEFT, PARTITIONED):
                    join (LEFT, PARTITIONED):
                        final aggregation over (d_year, ss_item_sk, ss_customer_sk)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [d_year, ss_item_sk, ss_customer_sk])
                                    intermediate aggregation over (d_year, ss_item_sk, ss_customer_sk)
                                        join (INNER, REPLICATED):
                                            partial aggregation over (ss_item_sk, ss_customer_sk, ss_sold_date_sk)
                                                join (LEFT, PARTITIONED):
                                                    remote exchange (REPARTITION, HASH, [ss_ticket_number, ss_item_sk])
                                                        dynamic filter (ss_sold_date_sk::EQUAL)
                                                            scan store_sales
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPARTITION, HASH, [sr_ticket_number, sr_item_sk])
                                                            scan store_returns
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    scan date_dim
                        final aggregation over (d_year_20, ws_item_sk, ws_bill_customer_sk)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [d_year_20, ws_item_sk, ws_bill_customer_sk])
                                    intermediate aggregation over (d_year_20, ws_item_sk, ws_bill_customer_sk)
                                        join (INNER, REPLICATED):
                                            partial aggregation over (ws_item_sk, ws_bill_customer_sk, ws_sold_date_sk)
                                                join (LEFT, PARTITIONED):
                                                    remote exchange (REPARTITION, HASH, [ws_order_number, ws_item_sk])
                                                        dynamic filter (ws_sold_date_sk::EQUAL)
                                                            scan web_sales
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPARTITION, HASH, [wr_order_number, wr_item_sk])
                                                            scan web_returns
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    scan date_dim
                    final aggregation over (d_year_61, cs_item_sk, cs_bill_customer_sk)
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPARTITION, HASH, [d_year_61, cs_item_sk, cs_bill_customer_sk])
                                intermediate aggregation over (d_year_61, cs_item_sk, cs_bill_customer_sk)
                                    join (INNER, REPLICATED):
                                        partial aggregation over (cs_item_sk, cs_bill_customer_sk, cs_sold_date_sk)
                                            join (LEFT, PARTITIONED):
                                                remote exchange (REPARTITION, HASH, [cs_order_number, cs_item_sk])
                                                    dynamic filter (cs_sold_date_sk::EQUAL)
                                                        scan catalog_sales
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [cr_order_number, cr_item_sk])
                                                        scan catalog_returns
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPLICATE, BROADCAST, [])
                                                scan date_dim
