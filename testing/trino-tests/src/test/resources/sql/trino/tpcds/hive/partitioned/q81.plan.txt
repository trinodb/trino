local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                cross join:
                    join (LEFT, REPLICATED):
                        join (INNER, PARTITIONED):
                            final aggregation over (ca_state, cr_returning_customer_sk)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [cr_returning_customer_sk])
                                        intermediate aggregation over (ca_state, cr_returning_customer_sk)
                                            join (INNER, PARTITIONED):
                                                remote exchange (REPARTITION, HASH, [ca_address_sk])
                                                    dynamic filter ([Reference[type=bigint, name=ca_address_sk]])
                                                        scan customer_address
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [cr_returning_addr_sk])
                                                        partial aggregation over (cr_returning_addr_sk, cr_returning_customer_sk)
                                                            join (INNER, REPLICATED):
                                                                dynamic filter ([Reference[type=bigint, name=cr_returning_customer_sk]], await [Reference[type=bigint, name=cr_returned_date_sk]])
                                                                    scan catalog_returns
                                                                local exchange (GATHER, SINGLE, [])
                                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                                        scan date_dim
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [c_customer_sk])
                                    join (INNER, REPLICATED):
                                        dynamic filter ([Reference[type=bigint, name=c_current_addr_sk]])
                                            scan customer
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPLICATE, BROADCAST, [])
                                                scan customer_address
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPLICATE, BROADCAST, [])
                                final aggregation over (ca_state_95)
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [ca_state_95])
                                            partial aggregation over (ca_state_95)
                                                final aggregation over (ca_state_95, cr_returning_customer_sk_31)
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPARTITION, HASH, [ca_state_95, cr_returning_customer_sk_31])
                                                            intermediate aggregation over (ca_state_95, cr_returning_customer_sk_31)
                                                                join (INNER, PARTITIONED):
                                                                    remote exchange (REPARTITION, HASH, [ca_address_sk_87])
                                                                        dynamic filter ([Reference[type=bigint, name=ca_address_sk_87]])
                                                                            scan customer_address
                                                                    local exchange (GATHER, SINGLE, [])
                                                                        remote exchange (REPARTITION, HASH, [cr_returning_addr_sk_34])
                                                                            partial aggregation over (cr_returning_addr_sk_34, cr_returning_customer_sk_31)
                                                                                join (INNER, REPLICATED):
                                                                                    dynamic filter (await [Reference[type=bigint, name=cr_returned_date_sk_51]])
                                                                                        scan catalog_returns
                                                                                    local exchange (GATHER, SINGLE, [])
                                                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                                                            scan date_dim
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPLICATE, BROADCAST, [])
                            single aggregation over ()
                                values (1 rows)
