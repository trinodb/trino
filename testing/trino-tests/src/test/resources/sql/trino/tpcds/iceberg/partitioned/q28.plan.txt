cross join:
    cross join:
        cross join:
            cross join:
                cross join:
                    final aggregation over ()
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (GATHER, SINGLE, [])
                                partial aggregation over ()
                                    final aggregation over (group, ss_list_price)
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPARTITION, HASH, [group, ss_list_price])
                                                partial aggregation over (group, ss_list_price)
                                                    scan store_sales
                    final aggregation over ()
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (GATHER, SINGLE, [])
                                partial aggregation over ()
                                    final aggregation over (group_164, ss_list_price_14)
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPARTITION, HASH, [group_164, ss_list_price_14])
                                                partial aggregation over (group_164, ss_list_price_14)
                                                    scan store_sales
                final aggregation over ()
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (GATHER, SINGLE, [])
                            partial aggregation over ()
                                final aggregation over (group_171, ss_list_price_46)
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [group_171, ss_list_price_46])
                                            partial aggregation over (group_171, ss_list_price_46)
                                                scan store_sales
            final aggregation over ()
                local exchange (GATHER, SINGLE, [])
                    remote exchange (GATHER, SINGLE, [])
                        partial aggregation over ()
                            final aggregation over (group_178, ss_list_price_78)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [group_178, ss_list_price_78])
                                        partial aggregation over (group_178, ss_list_price_78)
                                            scan store_sales
        final aggregation over ()
            local exchange (GATHER, SINGLE, [])
                remote exchange (GATHER, SINGLE, [])
                    partial aggregation over ()
                        final aggregation over (group_185, ss_list_price_110)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [group_185, ss_list_price_110])
                                    partial aggregation over (group_185, ss_list_price_110)
                                        scan store_sales
    final aggregation over ()
        local exchange (GATHER, SINGLE, [])
            remote exchange (GATHER, SINGLE, [])
                partial aggregation over ()
                    final aggregation over (group_192, ss_list_price_142)
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPARTITION, HASH, [group_192, ss_list_price_142])
                                partial aggregation over (group_192, ss_list_price_142)
                                    scan store_sales
