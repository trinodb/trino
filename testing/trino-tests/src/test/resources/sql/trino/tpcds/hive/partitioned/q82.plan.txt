local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                final aggregation over (i_current_price, i_item_desc, i_item_id)
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPARTITION, HASH, [i_current_price, i_item_desc, i_item_id])
                            intermediate aggregation over (i_current_price, i_item_desc, i_item_id)
                                join (INNER, REPLICATED, can skip output duplicates):
                                    partial aggregation over (ss_item_sk)
                                        dynamic filter ([ss_item_sk::bigint])
                                            scan store_sales
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPLICATE, BROADCAST, [])
                                            join (INNER, REPLICATED, can skip output duplicates):
                                                join (INNER, REPLICATED, can skip output duplicates):
                                                    dynamic filter (await [inv_date_sk::bigint, inv_item_sk::bigint])
                                                        scan inventory
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            scan date_dim
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        scan item
