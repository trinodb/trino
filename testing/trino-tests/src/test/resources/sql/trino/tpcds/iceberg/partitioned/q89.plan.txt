local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [i_category, i_brand, s_store_name, s_company_name])
                        final aggregation over (i_category, i_class, i_brand, s_store_name, s_company_name, d_moy)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [i_category, i_class, i_brand, s_store_name, s_company_name, d_moy])
                                    intermediate aggregation over (i_category, i_class, i_brand, s_store_name, s_company_name, d_moy)
                                        join (INNER, REPLICATED):
                                            join (INNER, REPLICATED):
                                                join (INNER, REPLICATED):
                                                    partial aggregation over (ss_store_sk, ss_sold_date_sk, ss_item_sk)
                                                        dynamic filter (ss_store_sk::EQUAL, ss_sold_date_sk::EQUAL, ss_item_sk::EQUAL)
                                                            scan store_sales
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            scan item
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        scan date_dim
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    scan store
