local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [i_manufact_id])
                        final aggregation over (d_qoy, i_manufact_id)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [d_qoy, i_manufact_id])
                                    partial aggregation over (d_qoy, i_manufact_id)
                                        join (INNER, REPLICATED):
                                            join (INNER, REPLICATED):
                                                join (INNER, REPLICATED):
                                                    alternatives
                                                        dynamic filter (await [ss_item_sk::bigint, ss_sold_date_sk::bigint, ss_store_sk::bigint])
                                                            scan store_sales
                                                        cache data
                                                            dynamic filter (await [ss_item_sk::bigint, ss_sold_date_sk::bigint, ss_store_sk::bigint])
                                                                scan store_sales
                                                        load from cache
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            alternatives
                                                                scan item
                                                                cache data
                                                                    scan item
                                                                load from cache
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        alternatives
                                                            scan date_dim
                                                            cache data
                                                                scan date_dim
                                                            load from cache
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    alternatives
                                                        scan store
                                                        cache data
                                                            scan store
                                                        load from cache
