cross join:
    cross join:
        cross join:
            cross join:
                cross join:
                    final aggregation over ()
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (GATHER, SINGLE, [])
                                partial aggregation over ()
                                    final aggregation over (group, ss_list_price)
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPARTITION, HASH, [group, ss_list_price])
                                                partial aggregation over (group, ss_list_price)
                                                    alternatives
                                                        scan store_sales
                                                        cache data
                                                            scan store_sales
                                                        load from cache
                    final aggregation over ()
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (GATHER, SINGLE, [])
                                partial aggregation over ()
                                    final aggregation over (group_147, ss_list_price_13)
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPARTITION, HASH, [group_147, ss_list_price_13])
                                                partial aggregation over (group_147, ss_list_price_13)
                                                    alternatives
                                                        scan store_sales
                                                        cache data
                                                            scan store_sales
                                                        load from cache
                final aggregation over ()
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (GATHER, SINGLE, [])
                            partial aggregation over ()
                                final aggregation over (group_154, ss_list_price_42)
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [group_154, ss_list_price_42])
                                            partial aggregation over (group_154, ss_list_price_42)
                                                alternatives
                                                    scan store_sales
                                                    cache data
                                                        scan store_sales
                                                    load from cache
            final aggregation over ()
                local exchange (GATHER, SINGLE, [])
                    remote exchange (GATHER, SINGLE, [])
                        partial aggregation over ()
                            final aggregation over (group_161, ss_list_price_71)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [group_161, ss_list_price_71])
                                        partial aggregation over (group_161, ss_list_price_71)
                                            alternatives
                                                scan store_sales
                                                cache data
                                                    scan store_sales
                                                load from cache
        final aggregation over ()
            local exchange (GATHER, SINGLE, [])
                remote exchange (GATHER, SINGLE, [])
                    partial aggregation over ()
                        final aggregation over (group_168, ss_list_price_100)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [group_168, ss_list_price_100])
                                    partial aggregation over (group_168, ss_list_price_100)
                                        alternatives
                                            scan store_sales
                                            cache data
                                                scan store_sales
                                            load from cache
    final aggregation over ()
        local exchange (GATHER, SINGLE, [])
            remote exchange (GATHER, SINGLE, [])
                partial aggregation over ()
                    final aggregation over (group_175, ss_list_price_129)
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPARTITION, HASH, [group_175, ss_list_price_129])
                                partial aggregation over (group_175, ss_list_price_129)
                                    alternatives
                                        scan store_sales
                                        cache data
                                            scan store_sales
                                        load from cache
