local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                join (INNER, PARTITIONED):
                    join (INNER, PARTITIONED):
                        final aggregation over (i_item_id)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [i_item_id])
                                    partial aggregation over (i_item_id)
                                        join (INNER, PARTITIONED):
                                            remote exchange (REPARTITION, HASH, [d_date])
                                                join (INNER, REPLICATED):
                                                    join (INNER, REPLICATED):
                                                        dynamic filter ([ss_item_sk::bigint], await [ss_sold_date_sk::bigint])
                                                            scan store_sales
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPLICATE, BROADCAST, [])
                                                                dynamic filter (unestimatable [i_item_id::varchar, i_item_id::varchar])
                                                                    scan item
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            dynamic filter (unestimatable [d_date::date])
                                                                scan date_dim
                                            final aggregation over (d_date_6)
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [d_date_6])
                                                        partial aggregation over (d_date_6)
                                                            join (INNER, REPLICATED, can skip output duplicates):
                                                                dynamic filter (await [d_week_seq_8::integer])
                                                                    scan date_dim
                                                                local exchange (GATHER, SINGLE, [])
                                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                                        local exchange (GATHER, SINGLE, [])
                                                                            remote exchange (GATHER, SINGLE, [])
                                                                                scan date_dim
                        final aggregation over (i_item_id_69)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [i_item_id_69])
                                    partial aggregation over (i_item_id_69)
                                        join (INNER, PARTITIONED):
                                            remote exchange (REPARTITION, HASH, [d_date_94])
                                                join (INNER, REPLICATED):
                                                    join (INNER, REPLICATED):
                                                        dynamic filter ([cs_item_sk::bigint], await [cs_sold_date_sk::bigint])
                                                            scan catalog_sales
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPLICATE, BROADCAST, [])
                                                                dynamic filter (unestimatable [i_item_id_69::varchar])
                                                                    scan item
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            dynamic filter (unestimatable [d_date_94::date])
                                                                scan date_dim
                                            final aggregation over (d_date_126)
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [d_date_126])
                                                        partial aggregation over (d_date_126)
                                                            join (INNER, REPLICATED, can skip output duplicates):
                                                                dynamic filter (await [d_week_seq_128::integer])
                                                                    scan date_dim
                                                                local exchange (GATHER, SINGLE, [])
                                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                                        local exchange (GATHER, SINGLE, [])
                                                                            remote exchange (GATHER, SINGLE, [])
                                                                                scan date_dim
                    final aggregation over (i_item_id_190)
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPARTITION, HASH, [i_item_id_190])
                                partial aggregation over (i_item_id_190)
                                    join (INNER, PARTITIONED):
                                        remote exchange (REPARTITION, HASH, [d_date_215])
                                            join (INNER, REPLICATED):
                                                join (INNER, REPLICATED):
                                                    dynamic filter ([ws_item_sk::bigint], await [ws_sold_date_sk::bigint])
                                                        scan web_sales
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            scan item
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        dynamic filter (unestimatable [d_date_215::date])
                                                            scan date_dim
                                        final aggregation over (d_date_247)
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPARTITION, HASH, [d_date_247])
                                                    partial aggregation over (d_date_247)
                                                        join (INNER, REPLICATED, can skip output duplicates):
                                                            dynamic filter (await [d_week_seq_249::integer])
                                                                scan date_dim
                                                            local exchange (GATHER, SINGLE, [])
                                                                remote exchange (REPLICATE, BROADCAST, [])
                                                                    local exchange (GATHER, SINGLE, [])
                                                                        remote exchange (GATHER, SINGLE, [])
                                                                            scan date_dim
