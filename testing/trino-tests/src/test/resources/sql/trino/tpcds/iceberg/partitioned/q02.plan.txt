remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            join (INNER, PARTITIONED):
                join (INNER, PARTITIONED):
                    single aggregation over (d_week_seq)
                        final aggregation over (d_day_name, d_week_seq)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [d_week_seq])
                                    intermediate aggregation over (d_day_name, d_week_seq)
                                        join (INNER, REPLICATED):
                                            local exchange (REPARTITION, ROUND_ROBIN, [])
                                                partial aggregation over (ws_sold_date_sk)
                                                    dynamic filter (ws_sold_date_sk::EQUAL)
                                                        scan web_sales
                                                partial aggregation over (cs_sold_date_sk)
                                                    dynamic filter (cs_sold_date_sk::EQUAL)
                                                        scan catalog_sales
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    dynamic filter (d_week_seq::EQUAL, d_week_seq::EQUAL)
                                                        scan date_dim
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPARTITION, HASH, [d_week_seq_21])
                            dynamic filter (d_week_seq_21::EQUAL)
                                scan date_dim
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [operator_subtract])
                        join (INNER, PARTITIONED):
                            single aggregation over (d_week_seq_127)
                                final aggregation over (d_day_name_137, d_week_seq_127)
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [d_week_seq_127])
                                            intermediate aggregation over (d_day_name_137, d_week_seq_127)
                                                join (INNER, REPLICATED):
                                                    local exchange (REPARTITION, ROUND_ROBIN, [])
                                                        partial aggregation over (ws_sold_date_sk_82)
                                                            dynamic filter (ws_sold_date_sk_82::EQUAL)
                                                                scan web_sales
                                                        partial aggregation over (cs_sold_date_sk_119)
                                                            dynamic filter (cs_sold_date_sk_119::EQUAL)
                                                                scan catalog_sales
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            dynamic filter (d_week_seq_127::EQUAL)
                                                                scan date_dim
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [d_week_seq_171])
                                    scan date_dim
