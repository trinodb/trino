final aggregation over ()
    local exchange (GATHER, SINGLE, [])
        remote exchange (GATHER, SINGLE, [])
            partial aggregation over ()
                final aggregation over (ws_order_number, group)
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPARTITION, HASH, [ws_order_number, group])
                            partial aggregation over (ws_order_number, group)
                                join (INNER, PARTITIONED):
                                    remote exchange (REPARTITION, HASH, [ws_ship_addr_sk])
                                        join (INNER, REPLICATED):
                                            join (INNER, REPLICATED):
                                                join (INNER, REPLICATED):
                                                    dynamic filter (ws_ship_addr_sk::EQUAL, ws_web_site_sk::EQUAL, ws_order_number::EQUAL, ws_ship_date_sk::EQUAL)
                                                        scan web_sales
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            scan date_dim
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        join (INNER, PARTITIONED):
                                                            final aggregation over (ws_order_number_28)
                                                                local exchange (GATHER, SINGLE, [])
                                                                    partial aggregation over (ws_order_number_28)
                                                                        join (INNER, PARTITIONED, can skip output duplicates):
                                                                            remote exchange (REPARTITION, HASH, [ws_order_number_28])
                                                                                dynamic filter (ws_order_number_28::EQUAL, ws_order_number_28::EQUAL)
                                                                                    scan web_sales
                                                                            local exchange (GATHER, SINGLE, [])
                                                                                remote exchange (REPARTITION, HASH, [ws_order_number_66])
                                                                                    dynamic filter (ws_order_number_66::EQUAL)
                                                                                        scan web_sales
                                                            final aggregation over (ws_order_number_112)
                                                                local exchange (GATHER, SINGLE, [])
                                                                    partial aggregation over (ws_order_number_112)
                                                                        join (INNER, PARTITIONED, can skip output duplicates):
                                                                            join (INNER, PARTITIONED, can skip output duplicates):
                                                                                remote exchange (REPARTITION, HASH, [ws_order_number_112])
                                                                                    dynamic filter (ws_order_number_112::EQUAL, ws_order_number_112::EQUAL)
                                                                                        scan web_sales
                                                                                local exchange (GATHER, SINGLE, [])
                                                                                    remote exchange (REPARTITION, HASH, [wr_order_number])
                                                                                        dynamic filter (wr_order_number::EQUAL)
                                                                                            scan web_returns
                                                                            local exchange (GATHER, SINGLE, [])
                                                                                remote exchange (REPARTITION, HASH, [ws_order_number_150])
                                                                                    scan web_sales
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    scan web_site
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [ca_address_sk])
                                            scan customer_address
