local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [expr_7, expr_8])
                        final aggregation over (i_category_gid, i_class_gid, groupid)
                            local exchange (REPARTITION, HASH, [i_category_gid, i_class_gid, groupid])
                                remote exchange (REPARTITION, HASH, [i_category_gid, i_class_gid, groupid])
                                    partial aggregation over (i_category_gid, i_class_gid, groupid)
                                        join (INNER, REPLICATED):
                                            join (INNER, REPLICATED):
                                                dynamic filter (ws_item_sk::EQUAL, ws_sold_date_sk::EQUAL)
                                                    scan web_sales
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        scan date_dim
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    scan item
