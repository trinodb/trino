local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, [expr])
                        join (FULL, PARTITIONED):
                            final aggregation over (d_date, ws_item_sk)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [ws_item_sk])
                                        intermediate aggregation over (d_date, ws_item_sk)
                                            join (INNER, REPLICATED):
                                                alternatives
                                                    partial aggregation over (ws_item_sk, ws_sold_date_sk)
                                                        dynamic filter (await [Reference[type=bigint, name=ws_sold_date_sk]])
                                                            scan web_sales
                                                    cache data
                                                        partial aggregation over (ws_item_sk, ws_sold_date_sk)
                                                            dynamic filter (await [Reference[type=bigint, name=ws_sold_date_sk]])
                                                                scan web_sales
                                                    load from cache
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        alternatives
                                                            scan date_dim
                                                            cache data
                                                                scan date_dim
                                                            load from cache
                            final aggregation over (d_date_9, ss_item_sk)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [ss_item_sk])
                                        intermediate aggregation over (d_date_9, ss_item_sk)
                                            join (INNER, REPLICATED):
                                                alternatives
                                                    partial aggregation over (ss_item_sk, ss_sold_date_sk)
                                                        dynamic filter (await [Reference[type=bigint, name=ss_sold_date_sk]])
                                                            scan store_sales
                                                    cache data
                                                        partial aggregation over (ss_item_sk, ss_sold_date_sk)
                                                            dynamic filter (await [Reference[type=bigint, name=ss_sold_date_sk]])
                                                                scan store_sales
                                                    load from cache
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                        alternatives
                                                            scan date_dim
                                                            cache data
                                                                scan date_dim
                                                            load from cache
