local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                cross join:
                    join (LEFT, REPLICATED):
                        join (INNER, REPLICATED):
                            final aggregation over (cr_returning_customer_sk, ca_state)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [cr_returning_customer_sk, ca_state])
                                        intermediate aggregation over (cr_returning_customer_sk, ca_state)
                                            join (INNER, PARTITIONED):
                                                remote exchange (REPARTITION, HASH, [cr_returning_addr_sk])
                                                    partial aggregation over (cr_returning_customer_sk, cr_returning_addr_sk)
                                                        join (INNER, REPLICATED):
                                                            dynamic filter (cr_returning_customer_sk::EQUAL, cr_returning_addr_sk::EQUAL, cr_returned_date_sk::EQUAL)
                                                                scan catalog_returns
                                                            local exchange (GATHER, SINGLE, [])
                                                                remote exchange (REPLICATE, BROADCAST, [])
                                                                    scan date_dim
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [ca_address_sk])
                                                        scan customer_address
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    join (INNER, REPLICATED):
                                        dynamic filter (c_current_addr_sk::EQUAL)
                                            scan customer
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPLICATE, BROADCAST, [])
                                                scan customer_address
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPLICATE, BROADCAST, [])
                                final aggregation over (ca_state_89)
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [ca_state_89])
                                            partial aggregation over (ca_state_89)
                                                final aggregation over (cr_returning_customer_sk_27, ca_state_89)
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPARTITION, HASH, [cr_returning_customer_sk_27, ca_state_89])
                                                            intermediate aggregation over (cr_returning_customer_sk_27, ca_state_89)
                                                                join (INNER, PARTITIONED):
                                                                    remote exchange (REPARTITION, HASH, [cr_returning_addr_sk_30])
                                                                        partial aggregation over (cr_returning_customer_sk_27, cr_returning_addr_sk_30)
                                                                            join (INNER, REPLICATED):
                                                                                dynamic filter (cr_returning_addr_sk_30::EQUAL, cr_returned_date_sk_47::EQUAL)
                                                                                    scan catalog_returns
                                                                                local exchange (GATHER, SINGLE, [])
                                                                                    remote exchange (REPLICATE, BROADCAST, [])
                                                                                        scan date_dim
                                                                    local exchange (GATHER, SINGLE, [])
                                                                        remote exchange (REPARTITION, HASH, [ca_address_sk_81])
                                                                            scan customer_address
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPLICATE, BROADCAST, [])
                            single aggregation over ()
                                values (1 rows)
