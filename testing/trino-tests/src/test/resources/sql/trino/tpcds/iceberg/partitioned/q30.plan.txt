local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                cross join:
                    join (LEFT, REPLICATED):
                        join (INNER, PARTITIONED):
                            final aggregation over (wr_returning_customer_sk, ca_state)
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPARTITION, HASH, [wr_returning_customer_sk])
                                        partial aggregation over (wr_returning_customer_sk, ca_state)
                                            join (INNER, PARTITIONED):
                                                remote exchange (REPARTITION, HASH, [wr_returning_addr_sk])
                                                    join (INNER, REPLICATED):
                                                        dynamic filter (wr_returning_customer_sk::EQUAL, wr_returning_addr_sk::EQUAL, wr_returned_date_sk::EQUAL)
                                                            scan web_returns
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPLICATE, BROADCAST, [])
                                                                scan date_dim
                                                local exchange (GATHER, SINGLE, [])
                                                    remote exchange (REPARTITION, HASH, [ca_address_sk])
                                                        scan customer_address
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, [c_customer_sk])
                                    join (INNER, REPLICATED):
                                        dynamic filter (c_current_addr_sk::EQUAL)
                                            scan customer
                                        local exchange (GATHER, SINGLE, [])
                                            remote exchange (REPLICATE, BROADCAST, [])
                                                scan customer_address
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPLICATE, BROADCAST, [])
                                final aggregation over (ca_state_86)
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [ca_state_86])
                                            partial aggregation over (ca_state_86)
                                                final aggregation over (wr_returning_customer_sk_27, ca_state_86)
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPARTITION, HASH, [wr_returning_customer_sk_27, ca_state_86])
                                                            partial aggregation over (wr_returning_customer_sk_27, ca_state_86)
                                                                join (INNER, PARTITIONED):
                                                                    remote exchange (REPARTITION, HASH, [wr_returning_addr_sk_30])
                                                                        join (INNER, REPLICATED):
                                                                            dynamic filter (wr_returning_addr_sk_30::EQUAL, wr_returned_date_sk_44::EQUAL)
                                                                                scan web_returns
                                                                            local exchange (GATHER, SINGLE, [])
                                                                                remote exchange (REPLICATE, BROADCAST, [])
                                                                                    scan date_dim
                                                                    local exchange (GATHER, SINGLE, [])
                                                                        remote exchange (REPARTITION, HASH, [ca_address_sk_78])
                                                                            scan customer_address
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPLICATE, BROADCAST, [])
                            single aggregation over ()
                                values (1 rows)
