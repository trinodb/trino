cross join:
    final aggregation over ()
        local exchange (GATHER, SINGLE, [])
            remote exchange (GATHER, SINGLE, [])
                partial aggregation over ()
                    join (INNER, REPLICATED):
                        join (INNER, REPLICATED):
                            join (INNER, REPLICATED):
                                alternatives
                                    dynamic filter (await [SymbolReference[type=bigint, name=ws_ship_hdemo_sk], SymbolReference[type=bigint, name=ws_sold_time_sk], SymbolReference[type=bigint, name=ws_web_page_sk]])
                                        scan web_sales
                                    cache data
                                        dynamic filter (await [SymbolReference[type=bigint, name=ws_ship_hdemo_sk], SymbolReference[type=bigint, name=ws_sold_time_sk], SymbolReference[type=bigint, name=ws_web_page_sk]])
                                            scan web_sales
                                    load from cache
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPLICATE, BROADCAST, [])
                                        alternatives
                                            scan web_page
                                            cache data
                                                scan web_page
                                            load from cache
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    alternatives
                                        scan time_dim
                                        cache data
                                            scan time_dim
                                        load from cache
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPLICATE, BROADCAST, [])
                                alternatives
                                    scan household_demographics
                                    cache data
                                        scan household_demographics
                                    load from cache
    final aggregation over ()
        local exchange (GATHER, SINGLE, [])
            remote exchange (GATHER, SINGLE, [])
                partial aggregation over ()
                    join (INNER, REPLICATED):
                        join (INNER, REPLICATED):
                            join (INNER, REPLICATED):
                                alternatives
                                    dynamic filter (await [SymbolReference[type=bigint, name=ws_ship_hdemo_sk_19], SymbolReference[type=bigint, name=ws_sold_time_sk_10], SymbolReference[type=bigint, name=ws_web_page_sk_21]])
                                        scan web_sales
                                    cache data
                                        dynamic filter (await [SymbolReference[type=bigint, name=ws_ship_hdemo_sk_19], SymbolReference[type=bigint, name=ws_sold_time_sk_10], SymbolReference[type=bigint, name=ws_web_page_sk_21]])
                                            scan web_sales
                                    load from cache
                                local exchange (GATHER, SINGLE, [])
                                    remote exchange (REPLICATE, BROADCAST, [])
                                        alternatives
                                            scan web_page
                                            cache data
                                                scan web_page
                                            load from cache
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPLICATE, BROADCAST, [])
                                    alternatives
                                        scan time_dim
                                        cache data
                                            scan time_dim
                                        load from cache
                        local exchange (GATHER, SINGLE, [])
                            remote exchange (REPLICATE, BROADCAST, [])
                                alternatives
                                    scan household_demographics
                                    cache data
                                        scan household_demographics
                                    load from cache
