local exchange (GATHER, SINGLE, [])
    remote exchange (GATHER, SINGLE, [])
        local exchange (GATHER, SINGLE, [])
            local exchange (REPARTITION, ROUND_ROBIN, [])
                final aggregation over (s_store_name, s_store_id)
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPARTITION, HASH, [s_store_name, s_store_id])
                            partial aggregation over (s_store_name, s_store_id)
                                final aggregation over (d_day_name, s_store_name, s_store_id)
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, [d_day_name, s_store_name, s_store_id])
                                            intermediate aggregation over (d_day_name, s_store_name, s_store_id)
                                                join (INNER, REPLICATED):
                                                    join (INNER, REPLICATED):
                                                        partial aggregation over (ss_store_sk, ss_sold_date_sk)
                                                            dynamic filter (ss_store_sk::EQUAL, ss_sold_date_sk::EQUAL)
                                                                scan store_sales
                                                        local exchange (GATHER, SINGLE, [])
                                                            remote exchange (REPLICATE, BROADCAST, [])
                                                                scan date_dim
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            scan store
