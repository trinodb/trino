# Release 444 (29 Mar 2024)

## Docker image

* Update Java runtime to Java 22. ({issue}`21161`)

## ClickHouse connector

* Improve performance of reading table comments.  ({issue}`21161`)

## Delta Lake connector

* Add support for reading `BYTE_STREAM_SPLIT` encoding in Parquet files. ({issue}`8357`)
* Add support for canned ACLs with the native S3 file system. ({issue}`21176`)

## Druid connector

* Improve performance of reading table comments.  ({issue}`21161`)

## Hive connector

* Add support for reading `BYTE_STREAM_SPLIT` encoding in Parquet files. ({issue}`8357`)
* Add support for canned ACLs with the native S3 file system. ({issue}`21176`)

## Hudi connector

* Add support for reading `BYTE_STREAM_SPLIT` encoding in Parquet files. ({issue}`8357`)
* Add support for canned ACLs with the native S3 file system. ({issue}`21176`)

## Iceberg connector

* Add support for the `metadata_log_entries` system table. ({issue}`20410`)
* Add support for reading `BYTE_STREAM_SPLIT` encoding in Parquet files. ({issue}`8357`)
* Add support for canned ACLs with the native S3 file system. ({issue}`21176`)

## Ignite connector

* Improve performance of reading table comments.  ({issue}`21161`)

## MariaDB connector

* Improve performance of reading table comments.  ({issue}`21161`)

## MySQL connector

* Improve performance of reading table comments.  ({issue}`21161`)

## Oracle connector

* Improve performance of reading table comments.  ({issue}`21161`)

## PostgreSQL connector

* Improve performance of reading table comments.  ({issue}`21161`)

## Redshift connector

* Improve performance of reading table comments.  ({issue}`21161`)

## SingleStore connector

* Improve performance of reading table comments.  ({issue}`21161`)

## Snowflake connector

* Add support for top-N pushdown. ({issue}`21219`)
* Add support for `avg` aggregation pushdown on `bigint` type. ({issue}`21148`)
* Improve performance of reading table comments.  ({issue}`21161`)

## SQLServer connector

* Improve performance of reading table comments.  ({issue}`21161`)